# Pins pytest's rootdir to tests/. Without this, pytest collects the plugin's
# root __init__.py as a package and its relative import fails when the repo
# directory name is not a valid Python identifier (e.g. on CI:
# "hermes-plugin-rocketchat"). Run the suite as: pytest tests/
[pytest]
